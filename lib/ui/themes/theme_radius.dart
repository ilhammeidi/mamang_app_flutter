import 'package:flutter/material.dart';

class ThemeRadius {
  static BorderRadius small = BorderRadius.circular(10);
  static BorderRadius medium = BorderRadius.circular(20);
  static BorderRadius big = BorderRadius.circular(50);
}